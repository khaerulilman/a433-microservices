apiVersion: v1
kind: Service
metadata:
  name: karsajobs-backend-service
spec:
  selector:
    app: karsajobs-backend
  ports:
    - port: 3000
      targetPort: 8080
      nodePort: 30001
  type: NodePort
